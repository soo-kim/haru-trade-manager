# 키움 픽스처 샘플

이 디렉토리는 키움 REST 응답 포맷 회귀 테스트용 샘플 JSON을 보관한다.

- `stkinfo_*`: 현재가/서버시간 조회 응답
- `order_*`: 주문/주문상태 조회 응답
- `candles_*`: 분봉/일봉 조회 응답

원칙:

1. 민감값(계좌번호/토큰)은 포함하지 않는다.
2. 실응답을 추가할 때는 필수 필드만 남기고 익명화한다.
3. 파서 변경 시 fixture 테스트(`test_kiwoom_fixtures_phase14.py`)를 반드시 통과해야 한다.
